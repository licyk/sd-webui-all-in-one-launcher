<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        Title="使用帮助" Width="760" Height="560" MinWidth="680" MinHeight="480" WindowStartupLocation="CenterScreen">
  <Grid Margin="18">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
      <StackPanel>
        <TextBlock Text="启动器是做什么的？" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,10"/>
        <TextBlock Text="这个 GUI 是 SD WebUI All In One Installer 的图形入口。它本身不直接安装 WebUI，而是帮你选择 WebUI / 工具、整理安装参数、重新下载安装器脚本，并用 PowerShell 打开上游 installer 来完成安装、修复、启动和维护。"
                   TextWrapping="Wrap" LineHeight="24" Margin="0,0,0,18"/>

        <TextBlock Text="推荐流程" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <TextBlock Text="1. 先到「软件选择」选择要安装或管理的 WebUI / 工具。&#x0a;2. 到「高级选项 > 安装路径」确认目标目录；如果系统里已经有安装目录，可以用搜索功能接管。&#x0a;3. 到「安装器设置」调整分支、镜像、代理或其他安装参数。&#x0a;4. 回到「一键启动」：未安装时运行安装器；已安装后选择 launch.ps1 启动，或选择 update.ps1 / terminal.ps1 / version_manager.ps1 做维护。"
                   TextWrapping="Wrap" LineHeight="24" Margin="0,0,0,18"/>

        <TextBlock Text="什么时候看哪份文档？" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <TextBlock Text="如果你想了解这个启动器里的按钮、路径、安装/启动模式、日志和配置位置，请打开「启动器说明」。如果 WebUI 已经启动，但不知道模型、提示词、文生图、图生图、扩展或常见报错怎么处理，请阅读 SD Note 教程。"
                   TextWrapping="Wrap" LineHeight="24" Margin="0,0,0,18"/>

        <Border Background="#EEF5FAFF" BorderBrush="#C8D7DCE2" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,4">
          <TextBlock Text="PowerShell 脚本会在独立控制台中运行。脚本失败时控制台会停留，优先看控制台输出；启动器自身问题可在「设置」中查看最近日志或打开日志文件夹。" TextWrapping="Wrap" LineHeight="22"/>
        </Border>
      </StackPanel>
    </ScrollViewer>
    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
      <Button Name="OpenLauncherDocBtn" Content="打开启动器说明" Width="140" Margin="0,0,8,0"/>
      <Button Name="OpenSdNoteBtn" Content="打开 WebUI 教程" Width="140" Margin="0,0,8,0"/>
      <Button Name="CloseBtn" Content="关闭" Width="90"/>
    </StackPanel>
  </Grid>
</Window>
